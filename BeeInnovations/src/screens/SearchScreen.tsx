import { View, Text } from 'react-native';
import React from 'react';
import Container from '../components/Container';

const SearchScreen = ({ navigation }: any) => {
    return (
        <Container back>
            <Text>Tim kiem bai biet on </Text>
        </Container>
    );
};

export default SearchScreen;